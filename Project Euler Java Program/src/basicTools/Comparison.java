package basicTools;

import java.util.HashSet;

public class Comparison {
	
	public static boolean isPermutation(Integer x, Integer y) {
		
		String[] xS, yS;
		
		Digit {
			
			String value;
			
			Digit(String value) {
				
			}
		}
		
		xS = x.toString().split("");
		yS = y.toString().split("");
		
		if (xS.length != yS.length) {
			return(false);
		}
		
		HashSet<Integer> firstSet = new HashSet<Integer>();
		
		for(String i : xS) firstSet.add(i);
	}

}
